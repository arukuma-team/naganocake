<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h3>カテゴリー編集</h3>
  		<%= form_with model: @category,url:admins_category_path,local:true do |f| %>
        <div class="form-group">
          <%= f.text_field :category, placeholder:"※ケーキ" %>
          <%= f.submit "変更", class: 'btn-sm btn-primary' %>
        </div>
      <% end %>
  		<%= link_to '戻る', admins_categories_path, class: "back" %>
  </div>
</div>